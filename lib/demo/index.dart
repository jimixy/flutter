import 'package:flutter/material.dart';
import 'input.dart';

class IndexPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: InputDemo());
  }
}
